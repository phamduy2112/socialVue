import { setActivePinia, createPinia } from "pinia";
import { describe, it, expect, beforeEach } from "vitest";

describe("User Store", () => {
  beforeEach(() => {
    setActivePinia(createPinia()); // Reset store mỗi lần test
  });

  it("should set user when login", () => {
  
  
  });
});